[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell"]
# Target platforms to build apps for (Rust target-triple syntax)
# Matches Tech Spec §13 exactly: musl-only on Linux (HC-2, fully static, no
# glibc build), aarch64-apple-darwin, x86_64-apple-darwin (best-effort).
# Windows is explicitly deferred (Requirements §13 — no Landlock/Seatbelt
# equivalent) and deliberately excluded, not an oversight.
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-musl", "x86_64-apple-darwin", "x86_64-unknown-linux-musl"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Where to host releases
hosting = "github"
# Whether to install an updater program
install-updater = false
